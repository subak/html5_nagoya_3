namespace :sass do
  desc "compile sass"
  task :compile do
    sh "bundle exec compass compile --relative-assets"
  end

  desc "clean cache file"
  task :clean do
    sh "bundle exec compass clean"
  end
end